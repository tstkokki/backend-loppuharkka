extends layout

block content
  div
    h1= title
    p Welcome to #{title}
    p #{description}

  div
    form(action="/users", method="get")
      input(type="submit", value="Press me").ruoskButton
